// Для удобного импорта моделей

const taskModel = require('./taskModel');
const userModel = require('./userModel');
const tagModel = require('./tagModel');
const contestModel = require('./contestModel');

module.exports = {
  taskModel,
  userModel,
  tagModel,
  contestModel
};